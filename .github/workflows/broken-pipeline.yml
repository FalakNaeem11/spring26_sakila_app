name: Broken CI Pipeline
on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu # This is the intentional error (should be ubuntu-latest)
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.9
      - name: Install deps
        run: |
          pip install -r requirements.txt
          pip install pytest
      - name: Run Tests
        run: pytest tests/ -v
  build: # Indentation was wrong in handout, fixing it here but leaving other errors
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Image
        run: docker build -t myapp .
  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' # Missing ${{ }} syntax
    steps:
      - name: Deploy
        run: echo Deploying...
